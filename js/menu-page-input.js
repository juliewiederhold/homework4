/* 
	Separates render function calls to allow for
	the user to access both the menu page and
	order online page without running into repetitive 
	menu listings
*/
$(function() {
   render(com.dawgpizza.menu);
});